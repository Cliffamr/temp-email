name = "temp-email-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Email routing requires compatibility flags
compatibility_flags = ["nodejs_compat"]

[vars]
# These will be overridden by secrets in production
WEBHOOK_URL = "https://temp-email-arifmrs-projects.vercel.app/api/webhooks/inbound-email"

# Secrets (set via wrangler secret put WEBHOOK_SECRET)
# WEBHOOK_SECRET = "your-64-char-secret"
